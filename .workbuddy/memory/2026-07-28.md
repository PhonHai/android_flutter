# 2026-07-28 工作日志

- 在 `Android Kotlin 知识点.markdown` 第六章 StateFlow/SharedFlow 新增「View 层怎么收集 StateFlow（生命周期感知完整 Demo）」小节：方式一 View 体系用 `repeatOnLifecycle`（含依赖、Activity 完整代码、常见错误对比），方式二 Jetpack Compose 用 `collectAsStateWithLifecycle`（含 lifecycle-runtime-compose 依赖、Composable 代码、与旧 collectAsState 区别），并附「ViewModel 永远暴露 StateFlow，View 层按技术栈二选一」配对表。
- 同步在第五章 Flow 面试高频答案里加指向该 Demo 的交叉引用。
- 文件 +73/-1 行，未 commit。
