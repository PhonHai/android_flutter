# 2026-07-08 工作日志

## Android Kotlin 知识点文档补充
- 第八点「Scope Functions」的 `with` 示例原本只有一行 println，未体现「对同一对象多次操作」
- 已并入自包含示例：定义 `User(name, var age, var score)`，用 `with(user)` 连续修改 age、读取拼接、打印、最后返回汇总字符串
- 文件：`Android Kotlin 知识点.markdown`（约 1597 行）
- 注意：未 commit，等待用户决定是否提交
