# 2026-07-24 工作记录

## 整合：Android Kotlin 知识点.markdown 第五章 Flow 冷流讲解
- 用户提供了一份优质的「为什么 Flow 是冷流」讲解，要求整合到第五章 Flow（冷流），去掉重复，按「什么是Flow/为什么需要Flow/Flow怎么用/对比热流」组织。
- 现有「## 冷流的本质（为什么不消费不生产）」节与用户提供内容高度重叠（都讲懒启动/独立性/口诀），但用户版本更详细（含代码证明/比喻/设计原因/能变热流吗）。
- 用用户提供的版本替换原「冷流的本质」节，重组为五个小节：①什么是冷流（定义）②为什么Flow是冷流（惰性执行+独立性，含 runBlocking 代码证明）③形象比喻（点播视频vs电视直播）④为什么Kotlin设计成冷流（资源节约+上下文安全）⑤Flow能变热流吗（StateFlow/shareIn/stateIn，并指向第六章）。
- 去重：删除原有 newsFlow 两次 collect 代码示例（用 Random 示例更直观）、合并口诀、Java 对照整合进「独立性」段后。
- 保留前后节不变（「为什么需要Flow」「Flow=协程里的序列」「背压」「Flow vs LiveData」）。
- 尚未 commit。

## 更新：Android Kotlin 知识点.markdown 第四章 Coroutines（来自微信公众号文章）
- 用户提供微信公众号文章链接（协程深入思考），要求将核心内容整合到第四章。
- 文章核心观点：协程不是"轻量级线程"，而是"任务调度机制"；suspend≠切线程；delay不阻塞线程；CoroutineExceptionHandler捕获不到async异常。
- 新增 4 个板块：
  1. **协程的本质**（第 191 行）：Coroutine→Continuation→Dispatcher→Thread Pool→Thread 链路图 + 角色职责表
  2. **delay 为什么不阻塞线程**（第 285 行）：挂起 vs 阻塞对比表 + 代码示例 + 底层原理（定时回调模型）
  3. **协程与线程全面对比**（第 498 行）：7 维对比表（本质/创建成本/调度者/切换成本/数量上限/暂停方式/并发模型）
  4. **面试速查：高频反直觉问题**（第 512 行）：launch会创建线程吗/suspend会切线程吗/什么时候发生线程切换/CoroutineExceptionHandler为什么捕获不到async异常
- 扩展"面试高频"节：新增「协程挂起时线程在干什么」「launch创建线程吗/suspend切线程吗」「CoroutineExceptionHandler vs async」三条 Q&A。
- 尚未 commit。
