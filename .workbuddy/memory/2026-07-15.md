# 2026-07-15 工作日志

## 完善 Android Kotlin 知识点.markdown — 协程异常处理（十三章 CoroutineExceptionHandler）
- 用户要求完善 CoroutineExceptionHandler 示例代码，讲清"全局处理器如何与其它异常关联、实际项目怎么用、多 ViewModel 如何共用同一 handler 并在一处提示"
- 重写 `## CoroutineExceptionHandler` 子节（1380→约1524），新增：
  - 与 try-catch 的分工图（handler=安全网、try-catch=业务错误主路径）
  - 坑1：handler 只对根协程生效，装在子协程上无效（含正/误代码）
  - 坑2：handler 接不住 async 异常，需在 await() 处 try-catch
  - 实战三步走：① 单例 globalExceptionHandler + ErrorEventBus(SharedFlow) ② BaseViewModel 用 SupervisorJob+handler 的 safeScope（或 viewModelScope.launch(handler) 包装）③ 根 Activity lifecycleScope 订阅总线统一 Toast/Snackbar
  - Java 对标表（Thread.setDefaultUncaughtExceptionHandler / EventBus）
- 同步修正「面试高频」：澄清 handler 作为安全网（配 SupervisorJob+事件总线兜底意外崩溃），不替代 try-catch 处理业务错误
- 文档 2064 → 2192 行(+128)，代码围栏 158 偶数配对正常，未 commit
- 用户背景延续：8 年 Java，准备 Android 面试，偏好 Java 对照 + 实战代码 + 易记口诀

## 重写十三章「传播规则」子节（回应"手动 throw 不真实、崩溃 log 去哪"的疑惑）
- 用户疑惑：示例用手写 throw，但真实业务是业务逻辑代码崩溃，协程被取消后"兜底提示/log 该打印在哪看不出"
- 重写 `## 传播规则`（1347 起），要点：
  - 明确 throw 只是占位，真实异常来自业务调用（网络 IOException / `!!` NPE / 解析异常），传播机制相同
  - 规则1 用真实业务版（api.getUser 网络断），逐步说明：子抛→父取消→兄弟被取消→上抛 viewModelScope(SupervisorJob 不连累其它顶层)→无 catch 无 handler→平台默认处理器→**Android 上 App 崩溃(红屏)，堆栈在 Logcat tag AndroidRuntime**
  - 直接回答用户："不处理就变成应用崩溃，log 在 Logcat AndroidRuntime，App 已死，兜底非可选"
  - 规则3（核心）：兜底两姿势——A 预期错误在调用处 try-catch 转 UiState(日志+提示位置)；B 意外崩溃交全局 handler(见下一节)
  - 加"对照：不处理 vs 处理"代码，强调独立顶层 launch 避免一个崩连累另一个
- 文档 2192 → 2239 行(+47)，代码围栏 164 偶数配对正常，未 commit

## CoroutineExceptionHandler 定义位置澄清（globalExceptionHandler 在哪）
- 用户在「实战多 ViewModel 共用 handler」demo 中问：safeScope 里引用的 `globalExceptionHandler` 在哪定义的？
- 在 `## 实战` 第一步之后新增引用说明块：globalExceptionHandler 与 ErrorEventBus 是**顶层声明(top-level，写在独立文件如 core/GlobalExceptionHandler.kt，不在任何 class 内)**；编译后变成 `GlobalExceptionHandlerKt.globalExceptionHandler`，属整个 module 全局可见；BaseViewModel 同 module 同包(或 import)即可直接引用
- 文档 2239 → 2258 行(+19)，已固化到文档

## 新增十二章「Job 家族」子节（回应 SupervisorJob 同类变体疑问）
- 用户在协程章节问 SupervisorJob 是干啥的、还有没有别的 XXXJob
- 在 `## 父子协程的取消传播` 之后、`## 面试高频` 之前新增 `## Job 家族：Job()/SupervisorJob()/Deferred/CompletableJob`，含 8 节：
  - 四成员一句话区分表(Job / Job() / SupervisorJob() / Deferred<T>)
  - Job() vs SupervisorJob() 取消传播对比(A 连坐 / B 各自为政) + Java 视角(Future/Executor 对照)
  - ⚠️最大坑：SupervisorJob 只保护直接子协程，孙协程崩仍连累；深层互不连坐用 supervisorScope{}
  - Deferred<T> 与 CompletableJob(手动 complete)
  - Job 状态机(New→Active→Completing→Completed / Cancelling→Cancelled) + isActive/isCompleted/isCancelled
  - 常用 API(cancel/cancelChildren/join/invokeOnCompletion)
  - Java 对照表(Future/CompletableFuture)
  - 实战：viewModelScope/lifecycleScope 内部是 SupervisorJob，多 launch 互不连坐
- 文档 2258 → 2359 行(+101)，代码围栏 178 偶数配对正常，未 commit
- 累计：今日多次文档增强，全部未 commit（等用户指令统一提交）

## 增强 Job 家族「CompletableJob 场景」与「状态机详解」(回应两个问题)
- 用户问:CompletableJob 的 complete()/invokeOnCompletion 有什么用、什么业务场景;Job 状态机流程不懂
- 增强 `### 4`:补 CompletableDeferred 当"未来事件开关"场景(等登录成功再拉主页, loginReady.complete(user)+await());invokeOnCompletion 区分 cause(null=正常/CancellationException=取消/else=异常)
- 增强 `### 5`:状态机逐状态拆解表(6 状态对照中文 新建/活动/完成中/已完成/取消中/已取消)+ 两条主线(正常 New→Active→Completing→Completed；取消 任意活跃阶段 cancel→Cancelling→Cancelled)+ 三属性真相(isCompleted 在取消时也为 true)
- 回复中用 show_widget 画了 Job 状态机 SVG 流转图辅助理解(正常线蓝绿/取消线琥珀红)
- 文档 2359 → 2420 行(+61)，代码围栏 182 偶数配对正常，未 commit
