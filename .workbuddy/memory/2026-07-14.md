# 2026-07-14 工作日志

## 深度完善 Android Kotlin 知识点.markdown（参考扔物线风格）
- 用户要求参考"抛物线博主"（朱凯 rengwuxian.com）完善协程/Flow/扩展函数/密封类，让懂 Java 者易懂易记
- 协程章节(十一)：新增「挂起的真正含义(扔物线视角)」+「suspend 底层 CPS 状态机转换」，含 Java Thread/runOnUiThread 对照、状态机伪代码、口诀
- Flow 章节(十四)：新增「冷流的本质」「背压：suspend 天然解决」「与 RxJava 对照表」，含 Java Stream 对照、buffer/conflate/collectLatest、RxJava 映射表
- 扩展函数(六)：深化「原理」子节，加反编译 Java 代码(StringKt 静态方法+接收者首参)、Java 调用、静态分派示例、扩展属性、取代 Utils 类
- 密封类(五)：新增「Java 真的没有密封类吗(Java 17 sealed 对照)」+「穷举的原理 & 易记口诀」，含 Java 17 sealed interface/record 对照、穷举编译期检查
- 文档 1651 → 1907 行(+256)，代码围栏配对正确(132 偶数)
- 用户背景：8 年 Java，准备 Android 面试；偏好 Java 对照 + 反编译 + 易记口诀的学习方式

## 协程章节新增「CoroutineScope 作用域核心知识」大节
- 用户自述卡在作用域理解，要求补全"作用域所有核心知识"
- 插入位置：十一章「三个核心构建块」之后、「Dispatchers 调度器」之前（新增 1058 行起）
- 7 个子节：① 什么是 CoroutineScope(=CoroutineContext，Java 对标"带生命周期的线程池") ② CoroutineContext 四件套(Job/Dispatcher/Name/Handler) ③ 为什么需要作用域(结构化并发三大保证) ④ 内置作用域一览(GlobalScope/viewModelScope/lifecycleScope/rememberCoroutineScope/runBlocking) ⑤ 作用域×调度器(默认 Main，withContext 临时切 IO 自动回 Main，直接回答用户问题2) ⑥ runBlocking(阻塞入口，Java CountDownLatch 对照) ⑦ 易记口诀
- 直接纠正用户问题2误区：withContext 必须处协程中，Activity 用 lifecycleScope；viewModelScope 与 lifecycleScope 是同一机制绑定不同生命周期
- 文档 1907 → 2064 行(+157)，代码围栏 148 偶数配对正常，未 commit
