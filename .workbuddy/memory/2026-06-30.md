# 2026-06-30 工作日志

## Skills Harness V4 部署
- 从 `~/haiphon/skills-runtime-v4/` 将 Skills Harness V4 部署到项目 `.skills/` 目录
- 共部署 8 个 skill：arch-rules, code-locate, coding-rules, confirm-first, git-commit, lazy-build, project-memory, verify-loop
- 同时部署了 `_harness/` 强制检查脚本（diff_guard, ai_smell_check, anchor_check, sk.sh）
- 部署了 `templates/`（project-map/progress/project-skill 模板）
- 更新 `skills.config.yaml`：`project_type: mobile`, `platform: workbuddy`，启用 confirm-first + verify-loop
- 创建 `.skills/INDEX.md` 技能索引文件
- 备份原配置为 `skills.config.yaml.bak`

## arch-rules 项目专用改造
- 将 arch-rules 从车机通用版改造为本项目专用版（Android 三套对照 + Flutter Add-to-App + 手机端）
- **其他 7 个 skill 完全不动**（用户明确要求）
- 改动清单：
  - `HEADER.md`：描述从"Car API/Launcher/Settings"改为"MethodChannel/FlutterEngine/NavHost/Room"
  - `SKILL.md`：重写为项目专用，加入三套代码对照修改原则、教学注释保护规则
  - `universal.md`：移除车机 ANR 阈值/AOSP 引用，改为三套子项目通用规则
  - `mobile.md`：从通用手机规则重写为三套对照架构（legacy/jetpack/native + flutter_module）
  - `flutter-hybrid.md`（新增）：FlutterEngine 生命周期、MethodChannel 契约、AAR 构建、架构边界
  - `automotive.md`（删除）：车机规则，本项目不适用
- `skills.config.yaml`：新增 `arch_rules_extra_conventions: [flutter-hybrid]` 配置

## 最佳实践 Skill 新建
- 从 workbuddy marketplace 安装了 `flutter-dev` 和 `android-native-dev`
- 基于 Google/Flutter 官方文档，创建了两个项目专用最佳实践 skill：
  - **jetpack-compose-mvvm**：Compose UDF、StateFlow+ViewModel、Sealed Class UI State、State Hoisting、Side Effects、反模式、本项目对照检查
  - **flutter-best-practices**：分层架构（UI/Data）、不可变数据模型、Repository 模式、UDF、Widget 规范、DI、导航、命名约定、测试、本项目对照检查
- 来源：[Compose UI Architecture](https://developer.android.com/develop/ui/compose/architecture) + [Flutter Architecture Recommendations](https://docs.flutter.dev/app-architecture/recommendations)
- 更新 `skills.config.yaml` + `INDEX.md`

## Skills Kit 打包
- 打包为 `pomodoro-skills-kit/` 可移植部署包（47 文件）
- 包含 `deploy.sh` 一键部署脚本 + `README.md`
- 配置模板化：只需改 `arch_rules_extra_conventions`
- 归档: `pomodoro-skills-kit.tar.gz` (75KB)

## 精简 + 同步项目
- 去除 kit 和项目中的所有 automotive/车机 残留
- `INDEX.md` 去掉 Marketplace Skill 章节、"三套对照"项目专用措辞
- `skills.config.yaml` 去掉 Harness V4 品牌、精简注释
