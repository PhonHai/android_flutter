# 2026-06-29 工作日志

- legacy-android 首页改造为底部 4 Tab 传统 Fragment 切换：
  - 新增 TabContainerFragment，内部用 FragmentTransaction hide/show 管理 4 个 Tab；
  - 新增 StatsFragment、SettingsFragment、AboutFragment 三个空白占位 Tab；
  - 修改 nav_graph.xml 将 TabContainerFragment 设为 startDestination；
  - TimerFragment 改为 TabContainer 第一个子 Tab，保留全部计时和导航入口逻辑；
  - CommentFragment「回到首页」返回目标改为 tabContainerFragment；
  - legacy-android BUILD SUCCESSFUL。
- 【崩溃修复】四级导航终点「返回首页」崩溃：Fragment already added: TimerFragment
  - 原因：popBackStack 恢复 TabContainerFragment 时 savedInstanceState==null，
    但 ChildFragmentManager 已保留旧的子 Fragment，initTabs 又 add 了一次
  - 修复：改为始终 findFragmentByTag 查找已存在实例，只在全部找不到时才 new+add
  - BUILD SUCCESSFUL
- 【UI修复】列表页内容被 Toolbar 盖住：activity_main.xml 根布局添加 android:fitsSystemWindows="true"
  - 原因：NavHostFragment 未考虑系统窗口inset（状态栏+ActionBar），Fragment内容从屏幕顶部开始
  - BUILD SUCCESSFUL
- 【文档】更新 .agent/project-map.md 项目地图：
  - 改为三套对照学习体系（legacy / jetpack / flutter）
  - 新增完整路由链路图、首页 Tab 架构详解
  - 新增关键技术对照表（状态管理、数据层、导航传参、返回栈管理）
- jetpack-android 同步改造为底部 4 Tab 首页：
  - 新增 MainTabScreen.kt：Compose Scaffold + NavigationBar 底部 4 Tab
  - AppNavHost startDestination 改为 "main" 路由指向 MainTabScreen
  - TimerScreen: onViewHistory→onEnterNavDemo，按钮文字「进入4级导航演示」
  - CommentScreen: popBackStack("main", false)，按钮文字「回到首页」
  - jetpack-android BUILD SUCCESSFUL
- native-android 同步改造为底部 4 Tab 首页：
  - 新增 AppNavHost.kt / MainTabScreen.kt + 4 级导航页面（home/list/detail/comment）
  - 添加 navigation-compose 依赖
  - Tab 1: 「启动 Flutter 番茄钟」按钮（startActivity → FlutterContainerActivity）+ 「进入4级导航演示」
  - 切换 LAUNCHER：FlutterContainerActivity → MainActivity（对齐 legacy/jetpack 模式）
  - native-android BUILD SUCCESSFUL
- 更新 .agent/project-map.md：补充 jetpack/native 路由链路图和 changelog
