# 2026-07-20 工作日志

- 用户反馈：Flow 章节写得糟糕看不懂，指出 `viewModel.latestNews` 和 `getLatestNews()` 脱节（冷流示例断层），且"只有 collect 才执行"表述误导。
- 已重写 `Android Kotlin 知识点.markdown` 第十四章「Flow = 协程里的序列」+「冷流的本质」两节：补全 Repository 造流 → ViewModel 用 `stateIn` 转 StateFlow → View 收流的完整数据链，明确 `latestNews` 就是 `getLatestNews()` 经 `stateIn` 转手的热流；澄清"冷流只有 collect 才执行"指的是"懒启动"而非"只发射一次"，`submitList` 实为每 ~5 秒触发一次。
- 改动尚未 commit。
