# 2026-07-01

## 优化：消除 onEnterNavDemo 回调传递（prop drilling）
- 改 `onEnterNavDemo: () -> Unit`（回调包装）→ 直接传 `navController: NavController` 参数
- 注意: Navigation Compose 2.8.x **没有** `LocalNavController`，必须通过参数传入
- 涉及文件（3 个）：
  - `TimerScreen.kt` — 删除 `onEnterNavDemo` 参数，新增 `navController: NavController` 参数
  - `MainTabScreen.kt` — 新增 `navController` 参数，传给 TimerScreen
  - `AppNavHost.kt` — 传 `navController` 给 MainTabScreen
- 构建验证: `./gradlew assembleDebug` → BUILD SUCCESSFUL
- 📝 **教训**: verify-loop 要求改完代码必须跑构建验证，不能漏

## 🔧 Skill 体系修复：加固编辑 SOP
- 问题: skill 是"建议式"约束，agent 可能跳过 verify-loop/confirm-first，用户不可见 = "盲盒"
- 修复:
  - `coding-rules/SKILL.md` 顶部新增 **🚨 强制编辑 SOP**：Step 0(confirm-first) → Step 1(Edit) → Step 2(verify-loop)，不可跳过
  - `verify-loop/SKILL.md` 新增**与 coding-rules SOP 关联**：跳过验证 = 任务未完成
  - agent 必须在回复中显式报告执行到哪一步
- 局限性(诚实): 这些仍是文本指令，没有硬性的 hook 拦截机制。最有效的保障是用户在对话里监督。
